# Probe transient execution frontend.
# The profile launcher may bind this configuration to the probe's execution
# service address. Persistent Gateway Quadlet ownership belongs to Gateway.

events {}

http {
  server {
    listen 8080;

    location /healthz {
      default_type text/plain;
      return 200 "probe\n";
    }
  }
}
